import 'package:flutter/material.dart';

class PresensiStatisticsPage extends StatelessWidget {
  const PresensiStatisticsPage({
    super.key,
    required String programId,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
